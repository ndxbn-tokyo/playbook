all:
  hosts:
    localhost:
  children:
    workstation:
      hosts:
        localhost:
    web:
      hosts:
        localhost:
    database:
      hosts:
        localhost:
    cache:
      hosts:
        localhost:
    message:
      hosts:
        localhost:
    monitor:
      hosts:
        localhost:
